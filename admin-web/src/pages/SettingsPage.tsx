export function SettingsPage() {
  return (
    <section className="panel">
      <h2>Runtime Settings</h2>
      <p>
        Placeholder route for current settings, revision history, publish, and
        rollback from <code>/api/admin/settings/*</code>.
      </p>
    </section>
  );
}
