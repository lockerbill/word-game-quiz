export function UsersPage() {
  return (
    <section className="panel">
      <h2>User Management</h2>
      <p>
        Placeholder route for listing users and updating role/status via
        <code> /api/admin/users/*</code>.
      </p>
    </section>
  );
}
